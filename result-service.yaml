apiVersion: v1
kind: Service
metadata:
  name: result-server-service
  namespace: exampleapp
spec:
  selector:
    app: result-server
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80
  type: ClusterIP
